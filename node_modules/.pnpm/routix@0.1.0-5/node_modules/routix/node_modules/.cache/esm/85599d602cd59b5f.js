_2cd‍.x([["pipe",()=>pipe],["identity",()=>identity],["escapeRe",()=>escapeRe],["noop",()=>noop],["split",()=>split],["Deferred",()=>Deferred],["stringHashCode",()=>stringHashCode]]);const pipe = (...fns) => x0 => fns.reduce((x, f) => f(x), x0)

       const identity = x => x

// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions#Escaping
       const escapeRe = string =>
  string.replace(/[.*+\-?^${}()|[\]\\]/g, '\\$&')

       const noop = () => {}

       const split = (predicate, items) => {
  const yes = []
  const no = []
  for (const item of items) {
    const target = predicate(item) ? yes : no
    target.push(item)
  }
  return [yes, no]
}

       const Deferred = () => {
  let resolve, reject
  const promise = new Promise((_resolve, _reject) => {
    resolve = _resolve
    reject = _reject
  })
  return { promise, resolve, reject }
}

       const stringHashCode = str => {
  let hash = 5381
  let i = str.length
  while (i--) hash = ((hash << 5) - hash) ^ str.charCodeAt(i)
  return (hash >>> 0).toString(36)
}
