/**
 * Rollup aggressive defaults for Svenchify.
 */

export default ({
  // Nollup is always watch
  watch = true,
  write = true,
  index = true,
  serve = {},
  ...options
}) => ({
  watch,
  write,
  index,
  serve: {
    nollup: 'localhost:42421',
    ...serve,
  },
  ...options,
})
